# 快速启动指南

## 第一步：安装依赖

```bash
npm install --legacy-peer-deps
```

**注意**：由于依赖版本冲突，必须使用 `--legacy-peer-deps` 标志。如果以后需要安装新的依赖，也请使用此标志。

## 第二步：启动项目

```bash
npm start
```

## 第三步：在手机上运行

1. 在手机上安装 **Expo Go** 应用
   - iOS: App Store 搜索 "Expo Go"
   - Android: Google Play 搜索 "Expo Go"

2. 启动后，终端会显示二维码
3. 用 Expo Go 扫描二维码即可运行

## 测试账号

由于是本地存储，你可以：

1. **注册新账号**
   - 点击"注册"
   - 输入任意邮箱和密码（至少6位）
   - 验证码会在弹窗中显示（例如：123456）

2. **登录**
   - 使用注册的邮箱和密码登录

## 功能测试流程

1. **创建群组**
   - 登录后，在首页点击"创建群组"
   - 输入群组名称和描述

2. **创建任务**
   - 点击群组进入详情
   - 切换到"任务"标签
   - 点击"创建任务"
   - 选择任务类型（文档/投票/讨论）

3. **邀请成员**
   - 在群组详情页，点击"邀请"
   - 输入成员邮箱（需要该邮箱已注册）
   - 成员在"消息"页面可以看到邀请

4. **测试任务**
   - **文档任务**：上传文档，标记完成
   - **投票任务**：添加选项，进行投票，查看结果
   - **讨论任务**：发表评论，回复评论

## 注意事项

- 所有数据存储在本地，应用进入后台时会清除
- 验证码是模拟的，会在弹窗中显示
- 文件上传使用本地 URI，不会真正上传到服务器

## 常见问题

**Q: 扫描二维码后无法连接？**
A: 确保手机和电脑在同一 WiFi 网络下

**Q: 安装依赖失败？**
A: 必须使用 `npm install --legacy-peer-deps`，因为 Expo SDK 54 的依赖需要此标志

**Q: 如何检查依赖版本是否正确？**
A: 运行 `npx expo install --check` 来检查所有依赖是否与 Expo SDK 54 兼容

**Q: 应用闪退？**
A: 检查控制台错误信息，确保所有依赖都已正确安装

